<%= form_for(@task) do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>
<%= f.label :details %>
<%= f.text_field :details %>
<div>
  <% unless @task.id.nil? %>
  <%= f.check_box :completed %>
  <%= f.label :completed %>
  <% end %>
  <%= f.submit %>
</div>
<% end %>
